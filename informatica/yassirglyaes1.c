/*CONTARE IL NUMERO DI CIFRE DI CUI SI COMPONE UN NUMERO DATO E
COMUNICARE QUANTE VOLTE COMPARE LA CIFRA DI VALORE K CHIESTA
ALL’UTENTE.
N.B.: accettare valori che vanno da 0 a 9999
ES: 3 è composto da 1 cifra e supponendo k=5 allora k compare 0 volte, 56 è composto
da 2 cifre e k compare 1 volta*/

#include<stdio.h>
int main(){

    int cifra=0;
    int k=0;
    
    printf("Inserisci un numero che va da 0 a 9999");
    scanf("%d", &cifra);

    if(cifra>=0 && cifra<=9999){
        printf=("il numero é accetabile")
    }
    else {
        printf("il numero non é accettabile");

    }
      
    
    if(cifra>=0 && cifra<10){
        printf=("il numero ha una cifra");

    }
    if(cifra>=10 && cifra<100){
        printf=("il numero ha due cifre");
    }
    if(cifra>=100 && cifra<1000){
        printf=("il numero ha tre cifre");
    }
    if(cifra>=1000 && cifra=<9999){
        printf=("il numero ha quattro cifre");
    }
    
    if(k=<9)





        

    
    
        
    

}