/*Scrivi un programma che ricerca i primi tre numeri perfetti e li visualizza sullo schermo
es: n=6 div=1 2 3*/

#include<stdio.h>

int main(){
    
    int n_perfetto;
    int sommadivisori;
    int cnt;
    int n;

    for(i=1; i<500; i++){
        if(n%i==0)
        sommadivisori+=i;
    }
        if (sommadivisori==i) {
            printf(" il mumero %d é perfetto \n", n);
            cnt++;
        }
        sommadivisori=0
}