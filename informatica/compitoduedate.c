/*DATE DUE DATE ESPRESSE IN g/m/aaaa DETERMINARE
LA DATA PIU RECENTE*/

#include <stdio.h>
int main(){

    int giorno=0;
    int mese=0;
    int anno=0;
    int giorno1=0;
    int mese1=0;
    int anno1=0;

    //richiesta degli input
    printf("Inserisci la prima data : ");
    scanf("%d", &giorno,&mese,&anno );
    printf("Inserisci la seconda data : ");
    scanf("%d" &giorno1,&mese1,&anno1);

    if(anno==0){
        printf("questa é la data più recente")
    }
    else{
        if(anno==0){
            if(mese==0){
            printf("questa é la data più recente")

        }
        
        }
    }
    else{
        if(anno==0){
            if(mese==0){
                if(giorno==0){
                    printf("questa é la data più recente");
                }
            }
        }
    }
   
     
}